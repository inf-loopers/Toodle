/**
 * @file LogoutButton.jsx
 * @description Button component that terminates the user's session with Auth0.
 *
 * Responsibilities:
 * - Invokes `logout()` from the `useAuth()` hook.
 * - Redirects the user back to window.location.origin on successful logout.
 * - Cleans up any cached session or local storage data.
 *
 * Expected Usage:
 * ```jsx
 * <LogoutButton variant="ghost" size="sm" />
 * ```
 */

export default function LogoutButton() {
  // TODO: Implement Auth0 session logout
  return null;
}
